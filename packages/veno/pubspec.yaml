name: veno
description: Flutter plugin.
homepage: https://github.com/qq15725/flutter-veno/tree/master/packages/veno
version: 0.0.3

dependencies:
  flutter:
    sdk: flutter
  veno_router: ^0.0.3
  veno_model: ^0.0.3
  veno_container: ^0.0.3
  shared_preferences: ^0.5.12+4

dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=2.1.0 <3.0.0"